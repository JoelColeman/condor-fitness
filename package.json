{
  "name": "condor-fitness-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only tooling for the Condor Fitness Supabase migration. The app itself is vanilla HTML/CSS/JS and has no build step.",
  "type": "module",
  "scripts": {
    "backfill": "tsx scripts/backfill-supabase.ts"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
